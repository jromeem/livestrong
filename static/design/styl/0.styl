//////////////////
// -- 0 BASE -- //
//////////////////

// kyles stuff -- from footer
Helvetica()
    font-family "HelveticaNeue", "Helvetica Neue", Helvetica, Arial, sans-serif
    font-weight normal
body, html {
    margin 0
    width 100%
    height 100%
    min-width 1024px
    Helvetica()
}
.wrapper {
    max-width 996px
    margin 0px auto
    margin-bottom 0px
    .wrapper__content {
        
    }
}
.livestrong-btn {
    background $yellow
    color $black
    &:hover, &:focus {
        background $black
        color $white
    }
}